spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/licence_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
server:
  port: 8080